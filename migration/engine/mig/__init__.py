"""Deterministic engine behind the alteryx-sdp-migrate skill.

Everything exhaustive, repetitive, or exact is done here in Python so the LLM
only ever sees small, task-scoped context packs. See ../../SKILL.md.
"""

__version__ = "1.0.0"
